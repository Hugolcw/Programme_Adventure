# C++计算导论与程序设计 - 期末复习指南

## 📚 课程结构概览

本课程共包含10个主要章节，从基础语法到高级特性：

1. **Lesson 1: 数据类型** - 基础语法与数据类型
2. **Lesson 2: 函数和作用域** - 函数定义、参数传递、作用域
3. **Lesson 3: 数组和指针** - 数组操作、指针使用
4. **Lesson 4: OOP上** - 类与对象、构造函数
5. **Lesson 5: OOP下** - 继承、访问控制
6. **Lesson 6: 多态** - 虚函数、动态绑定
7. **Lesson 7: 智能指针** - unique_ptr、shared_ptr、weak_ptr
8. **Lesson 8: STL容器** - vector、list、map、string等
9. **Lesson 9: STL算法** - sort、find、transform等
10. **Lesson 10: 移动语义** - 左值/右值、移动构造函数

---

## 📖 各章节重点知识点

### Lesson 1: 数据类型

#### 核心知识点
- **基本数据类型**
  - 整型：`short`, `int`, `long`, `long long`
  - 浮点型：`float`, `double`, `long double`
  - 字符型：`char`, `signed char`, `unsigned char`
  - 布尔型：`bool`
  - 字符串：`string`（C++风格）

- **变量声明与初始化**
  ```cpp
  int x = 10;        // 初始化
  int y;            // 未初始化（危险！）
  const int PI = 3.14;  // 常量
  ```

- **运算符**
  - 算术运算符：`+`, `-`, `*`, `/`, `%`
  - 关系运算符：`==`, `!=`, `<`, `>`, `<=`, `>=`
  - 逻辑运算符：`&&`, `||`, `!`
  - 赋值运算符：`=`, `+=`, `-=`, `*=`, `/=`

- **控制流语句**
  - `if-else`：条件判断
  - `for`：循环
  - `while`：循环
  - `switch-case`：多分支选择

- **输入输出**
  ```cpp
  #include <iostream>
  using namespace std;
  
  cin >> variable;      // 输入
  cout << value << endl; // 输出
  ```

#### 常见考点
- 数据类型的大小和范围（`sizeof`）
- 整数溢出问题
- 浮点数精度问题
- 字符与ASCII码的转换
- 布尔值的数值表示（0/1）

#### 练习文件
- `circle_calculator.cpp` - 圆面积计算
- `even_checker.cpp` - 偶数判断
- `multiples_of_three.cpp` - 3的倍数
- `calculator.cpp` - 四则运算计算器

---

### Lesson 2: 函数和作用域

#### 核心知识点
- **函数定义与声明**
  ```cpp
  // 函数声明
  int add(int a, int b);
  
  // 函数定义
  int add(int a, int b) {
      return a + b;
  }
  ```

- **参数传递方式**
  - **值传递**：复制参数值，不改变原变量
    ```cpp
    void func(int x) { x = 10; }  // 不影响原变量
    ```
  - **引用传递**：传递变量的引用，可以修改原变量
    ```cpp
    void func(int& x) { x = 10; }  // 修改原变量
    ```
  - **指针传递**：传递地址
    ```cpp
    void func(int* x) { *x = 10; }  // 通过指针修改
    ```

- **函数重载**
  - 同名函数，不同参数列表
  ```cpp
  int add(int a, int b);
  double add(double a, double b);
  ```

- **默认参数**
  ```cpp
  void func(int x, int y = 10);  // y有默认值
  ```

- **作用域**
  - 全局作用域：整个程序可见
  - 局部作用域：函数内部可见
  - 块作用域：`{}` 内部可见

- **命名空间**
  ```cpp
  using namespace std;  // 使用整个命名空间
  using std::cout;      // 使用特定标识符
  ```

#### 常见考点
- 值传递 vs 引用传递的区别
- 函数重载的规则（参数类型或数量不同）
- 默认参数的位置（必须从右到左）
- 局部变量 vs 全局变量
- 函数调用的栈帧概念

#### 关键代码示例
参考：`lesson2.function-and-scope/examples/functions_and_scope_example.cpp`

---

### Lesson 3: 数组和指针

#### 核心知识点
- **数组**
  ```cpp
  int arr[5] = {1, 2, 3, 4, 5};  // 声明并初始化
  int arr[5];                     // 声明
  arr[0] = 10;                   // 访问元素（索引从0开始）
  ```

- **指针基础**
  ```cpp
  int x = 10;
  int* ptr = &x;      // ptr指向x的地址
  *ptr = 20;          // 通过指针修改x的值
  ```

- **指针与数组**
  ```cpp
  int arr[5] = {1, 2, 3, 4, 5};
  int* ptr = arr;              // 数组名是指向首元素的指针
  cout << *(ptr + 1);          // 访问arr[1]
  cout << ptr[1];              // 等价写法
  ```

- **指针运算**
  - `ptr + 1`：指向下一个元素
  - `ptr - 1`：指向上一个元素
  - `ptr1 - ptr2`：两个指针之间的距离

- **动态内存分配**
  ```cpp
  int* ptr = new int;          // 分配单个int
  int* arr = new int[10];      // 分配数组
  delete ptr;                  // 释放单个
  delete[] arr;                // 释放数组
  ```

#### 常见考点
- 数组越界问题
- 指针的初始化（未初始化的指针很危险）
- `&`（取地址）和 `*`（解引用）的区别
- 数组名与指针的关系
- 动态内存的分配与释放（避免内存泄漏）

#### 关键代码示例
参考：`lesson3.array-and-pointer/examples/Arrays_and_Pointers.cpp`

---

### Lesson 4: OOP上 - 类与对象

#### 核心知识点
- **类的定义**
  ```cpp
  class Car {
  public:
      string color;
      string brand;
      int speed;
      
      void start() {
          cout << "汽车启动了！" << endl;
      }
  };
  ```

- **访问控制**
  - `public`：公有成员，外部可访问
  - `private`：私有成员，仅类内可访问
  - `protected`：保护成员，类内和派生类可访问

- **构造函数**
  - 默认构造函数：无参数
  - 带参构造函数：有参数
  - 构造函数重载：多个构造函数
  ```cpp
  class Car {
  public:
      Car() {                    // 默认构造函数
          color = "未设定";
          speed = 0;
      }
      
      Car(string _color, int _speed) {  // 带参构造函数
          color = _color;
          speed = _speed;
      }
  };
  ```

- **对象创建**
  ```cpp
  Car myCar;                          // 调用默认构造函数
  Car yourCar("红色", 100);          // 调用带参构造函数
  ```

- **成员函数**
  - Getter/Setter函数
  - 成员函数可以访问所有成员变量

#### 常见考点
- 构造函数的特点（与类名相同，无返回类型）
- 默认构造函数的作用
- 访问控制符的作用域
- 封装的概念（数据隐藏）
- 对象的内存布局

#### 关键代码示例
参考：
- `lesson4.OOP上/examples.public/car_class.cpp`
- `lesson4.OOP上/examples.public/car_constructor.cpp`

---

### Lesson 5: OOP下 - 继承

#### 核心知识点
- **继承语法**
  ```cpp
  class Student {
  protected:
      string name;
      int age;
  public:
      Student(string _name, int _age) {
          name = _name;
          age = _age;
      }
  };
  
  class Undergraduate : public Student {
  private:
      string major;
  public:
      Undergraduate(string _name, int _age, string _major)
          : Student(_name, _age) {    // 调用父类构造函数
          major = _major;
      }
  };
  ```

- **继承方式**
  - `public`：公有继承（最常用）
  - `private`：私有继承
  - `protected`：保护继承

- **成员访问**
  - 子类可以访问父类的 `public` 和 `protected` 成员
  - 子类不能直接访问父类的 `private` 成员

- **构造函数调用顺序**
  1. 父类构造函数
  2. 子类构造函数

- **析构函数调用顺序**
  1. 子类析构函数
  2. 父类析构函数

- **函数重写（Override）**
  ```cpp
  class Student {
  public:
      void displayInfo() { ... }
  };
  
  class Undergraduate : public Student {
  public:
      void displayInfo() {          // 重写父类函数
          Student::displayInfo();   // 调用父类版本
          // 添加额外信息
      }
  };
  ```

#### 常见考点
- 继承的访问控制
- 构造函数初始化列表的使用
- 父类和子类构造/析构函数的调用顺序
- `protected` 的作用
- 函数重写 vs 函数重载

#### 关键代码示例
参考：`lesson5.OOP下/examples/car_student.cpp`

---

### Lesson 6: 多态

#### 核心知识点
- **虚函数（Virtual Function）**
  ```cpp
  class Student {
  public:
      virtual void displayInfo() {    // 虚函数
          cout << "Student info" << endl;
      }
  };
  
  class Undergraduate : public Student {
  public:
      void displayInfo() override {    // 重写虚函数
          cout << "Undergraduate info" << endl;
      }
  };
  ```

- **多态的实现**
  - 通过基类指针或引用调用虚函数
  - 运行时根据实际对象类型决定调用哪个函数
  ```cpp
  Student* ptr = new Undergraduate(...);
  ptr->displayInfo();  // 调用Undergraduate的displayInfo
  ```

- **纯虚函数与抽象类**
  ```cpp
  class Shape {
  public:
      virtual void draw() = 0;  // 纯虚函数
  };
  // Shape是抽象类，不能实例化
  ```

- **虚析构函数**
  ```cpp
  class Base {
  public:
      virtual ~Base() { }  // 虚析构函数，确保正确释放资源
  };
  ```

#### 常见考点
- `virtual` 关键字的作用
- 多态的条件（基类指针/引用 + 虚函数）
- 虚函数表（vtable）的概念
- 为什么需要虚析构函数
- 纯虚函数与抽象类

#### 关键代码示例
参考：`lesson6.polymorphism/examples/student_polymorphism.cpp`

---

### Lesson 7: 智能指针

#### 核心知识点
- **为什么需要智能指针**
  - 自动管理内存，避免内存泄漏
  - 避免悬空指针

- **unique_ptr**
  - 独占所有权，不能复制，只能移动
  ```cpp
  #include <memory>
  
  unique_ptr<int> ptr = make_unique<int>(10);
  unique_ptr<int> ptr2 = move(ptr);  // 转移所有权
  ```

- **shared_ptr**
  - 共享所有权，使用引用计数
  ```cpp
  shared_ptr<int> ptr1 = make_shared<int>(10);
  shared_ptr<int> ptr2 = ptr1;  // 共享所有权，引用计数+1
  ```

- **weak_ptr**
  - 弱引用，不增加引用计数
  - 用于解决循环引用问题
  ```cpp
  weak_ptr<int> wptr = ptr1;
  if (auto locked = wptr.lock()) {  // 尝试获取shared_ptr
      // 使用locked
  }
  ```

- **原始指针的问题**
  ```cpp
  int* ptr = new int(10);
  // 如果忘记delete，会导致内存泄漏
  delete ptr;
  ```

#### 常见考点
- `unique_ptr` vs `shared_ptr` 的使用场景
- 引用计数的概念
- 循环引用问题及解决方案（weak_ptr）
- `make_unique` 和 `make_shared` 的优势
- RAII（资源获取即初始化）原则

#### 关键代码示例
参考：
- `lesson7.smart_pointer/examples/unique_ptr_exercise.cpp`
- `lesson7.smart_pointer/examples/circular_reference_weak_ptr.cpp`

---

### Lesson 8: STL容器

#### 核心知识点
- **vector（动态数组）**
  ```cpp
  #include <vector>
  
  vector<int> v;
  v.push_back(10);        // 添加元素
  v.pop_back();           // 删除最后一个元素
  v.size();               // 获取大小
  v.at(0);                // 安全访问（会检查边界）
  v[0];                   // 直接访问（不检查边界）
  v.clear();              // 清空
  ```

- **list（双向链表）**
  ```cpp
  #include <list>
  
  list<int> l;
  l.push_back(10);
  l.push_front(5);
  l.pop_back();
  l.pop_front();
  ```

- **map（有序映射）**
  ```cpp
  #include <map>
  
  map<string, int> m;
  m["apple"] = 5;
  m["banana"] = 3;
  m.find("apple");        // 查找
  m.count("apple");       // 计数（0或1）
  ```

- **unordered_map（哈希映射）**
  ```cpp
  #include <unordered_map>
  
  unordered_map<string, int> um;
  // 用法类似map，但无序，查找更快
  ```

- **string（字符串）**
  ```cpp
  #include <string>
  
  string s = "Hello";
  s += " World";
  s.length();
  s.substr(0, 5);         // 子串
  s.find("World");        // 查找
  ```

- **迭代器**
  ```cpp
  vector<int> v = {1, 2, 3, 4, 5};
  for (auto it = v.begin(); it != v.end(); ++it) {
      cout << *it << " ";
  }
  
  // 范围for循环（C++11）
  for (int val : v) {
      cout << val << " ";
  }
  ```

#### 常见考点
- `vector` vs `list` 的选择（随机访问 vs 插入删除）
- `map` vs `unordered_map` 的区别
- `at()` vs `[]` 的区别（边界检查）
- 迭代器的使用
- `push_back` vs `emplace_back`（C++11）

#### 关键代码示例
参考：
- `lesson8.STL_Containers/examples/vector_basics.cpp`
- `lesson8.STL_Containers/examples/map_basics.cpp`
- `lesson8.STL_Containers/examples/string_manipulation.cpp`

---

### Lesson 9: STL算法

#### 核心知识点
- **sort（排序）**
  ```cpp
  #include <algorithm>
  
  vector<int> v = {3, 1, 4, 1, 5};
  sort(v.begin(), v.end());              // 默认升序
  sort(v.begin(), v.end(), greater<int>()); // 降序
  
  // 自定义比较函数
  bool compare(int a, int b) {
      return a > b;
  }
  sort(v.begin(), v.end(), compare);
  ```

- **find（查找）**
  ```cpp
  auto it = find(v.begin(), v.end(), 3);
  if (it != v.end()) {
      cout << "找到了" << endl;
  }
  ```

- **find_if（条件查找）**
  ```cpp
  auto it = find_if(v.begin(), v.end(), 
      [](int x) { return x > 3; });
  ```

- **count（计数）**
  ```cpp
  int cnt = count(v.begin(), v.end(), 1);
  ```

- **transform（转换）**
  ```cpp
  vector<int> v1 = {1, 2, 3};
  vector<int> v2(3);
  transform(v1.begin(), v1.end(), v2.begin(),
      [](int x) { return x * 2; });
  ```

- **remove（移除）**
  ```cpp
  auto it = remove(v.begin(), v.end(), 1);
  v.erase(it, v.end());  // 真正删除
  ```

#### 常见考点
- 算法的通用性（适用于所有容器）
- 迭代器范围：`[begin, end)`
- Lambda表达式的使用（C++11）
- `remove` 不会真正删除元素，需要配合 `erase`
- 自定义比较函数/谓词

#### 关键代码示例
参考：
- `lesson9.STL_Algorithms/examples/algorithm_sort.cpp`
- `lesson9.STL_Algorithms/examples/algorithm_find.cpp`
- `lesson9.STL_Algorithms/examples/algorithm_transform.cpp`

---

### Lesson 10: 移动语义

#### 核心知识点
- **左值（Lvalue）和右值（Rvalue）**
  - 左值：有名字的变量，可以取地址
  - 右值：临时对象，字面量，不能取地址
  ```cpp
  int x = 10;        // x是左值
  int y = x + 5;     // x+5是右值
  ```

- **移动构造函数**
  ```cpp
  class Buffer {
  public:
      Buffer(Buffer&& other) noexcept {  // 移动构造函数
          _data = other._data;
          _size = other._size;
          other._data = nullptr;  // 清空原对象
          other._size = 0;
      }
  };
  ```

- **拷贝构造函数 vs 移动构造函数**
  - 拷贝：深拷贝，创建新资源
  - 移动：转移资源所有权，避免不必要的拷贝
  ```cpp
  Buffer(const Buffer& other);   // 拷贝构造函数
  Buffer(Buffer&& other);        // 移动构造函数
  ```

- **std::move**
  ```cpp
  Buffer b1(100);
  Buffer b2 = move(b1);  // 强制使用移动构造函数
  ```

#### 常见考点
- 左值引用 `&` vs 右值引用 `&&`
- 什么时候调用移动构造函数
- `std::move` 的作用
- 移动语义的性能优势
- `noexcept` 关键字

#### 关键代码示例
参考：
- `lesson10.move_semantics/examples/move_constructor.cpp`
- `lesson10.move_semantics/examples/lvalue_rvalue.cpp`

---

## 🎯 常见考点总结

### 1. 内存管理
- **栈内存**：局部变量，自动管理
- **堆内存**：`new/delete`，需要手动管理
- **智能指针**：自动管理堆内存

### 2. 指针相关
- 指针的初始化
- 指针的解引用 `*`
- 指针的取地址 `&`
- 空指针 `nullptr`
- 悬空指针问题

### 3. 面向对象
- 封装：`public/private/protected`
- 继承：`public/protected/private` 继承
- 多态：虚函数、动态绑定
- 构造函数/析构函数

### 4. STL使用
- 容器的选择（vector/list/map）
- 迭代器的使用
- 算法的通用性
- Lambda表达式

### 5. 常见错误
- 数组越界
- 内存泄漏（忘记delete）
- 悬空指针
- 未初始化的变量
- 函数参数传递方式错误

---

## 📝 复习建议

### 1. 理论复习
- [ ] 复习各章节的核心概念
- [ ] 理解面向对象的三大特性（封装、继承、多态）
- [ ] 掌握STL容器和算法的使用
- [ ] 理解内存管理机制

### 2. 代码实践
- [ ] 运行每个章节的示例代码
- [ ] 完成每个章节的练习题
- [ ] 理解代码的执行流程
- [ ] 尝试修改代码，观察结果

### 3. 重点章节
根据课程结构，重点复习：
1. **OOP相关**（Lesson 4-6）：类、继承、多态
2. **STL相关**（Lesson 8-9）：容器和算法
3. **内存管理**（Lesson 7）：智能指针
4. **基础语法**（Lesson 1-3）：确保基础扎实

### 4. 考试准备
- [ ] 准备常见题型的代码模板
- [ ] 练习手写代码（类定义、函数实现）
- [ ] 理解代码的输出结果
- [ ] 掌握常见错误的识别和修正

---

## 🔍 快速查找

### 按主题查找代码示例

**基础语法**
- 数据类型：`lesson1.data-type/examples/data_types_tutorial/`
- 函数：`lesson2.function-and-scope/examples/`
- 数组指针：`lesson3.array-and-pointer/examples/`

**面向对象**
- 类与对象：`lesson4.OOP上/examples.public/`
- 继承：`lesson5.OOP下/examples/`
- 多态：`lesson6.polymorphism/examples/`

**高级特性**
- 智能指针：`lesson7.smart_pointer/examples/`
- STL容器：`lesson8.STL_Containers/examples/`
- STL算法：`lesson9.STL_Algorithms/examples/`
- 移动语义：`lesson10.move_semantics/examples/`

---

## 💡 考试技巧

1. **仔细阅读题目**：理解需求，注意细节
2. **先写框架**：类定义、函数声明
3. **逐步实现**：先实现基本功能，再优化
4. **检查语法**：括号匹配、分号、作用域
5. **测试边界**：空指针、空容器、边界值
6. **代码规范**：命名清晰、注释适当

---

## 📚 推荐复习顺序

1. **第一轮**：快速浏览所有章节，回顾知识点
2. **第二轮**：重点复习OOP和STL章节
3. **第三轮**：做练习题，查漏补缺
4. **第四轮**：模拟考试，练习手写代码

---

**祝考试顺利！加油！** 🎉

